const express = require('express');
const router = express.Router();

// Placeholder - will build this later
router.get('/', (req, res) => {
  res.json({ message: 'Context route - coming soon' });
});

module.exports = router;